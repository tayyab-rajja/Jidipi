import {getPostCategories} from "../../../../helpers/changePostsData";

const Company = (prop: any) => {
   console.log(prop);

    return (
        <div>
            signel post
        </div>
    );
};
export default Company;